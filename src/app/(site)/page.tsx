// TODO(c): home dashboard thật (hero, LIVE NOW, upcoming, results, news).
// Chỉ placeholder để route / compile — Gói C sở hữu file này.
export default function HomePage() {
  return (
    <div className="mx-auto max-w-7xl px-4 py-10">
      <h1 className="text-3xl font-bold">Sport</h1>
      <p className="mt-2 text-muted-foreground">
        Tỷ số trực tiếp, lịch thi đấu, kết quả và tin tức thể thao.
      </p>
    </div>
  );
}
